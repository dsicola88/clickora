/** Largura útil padrão do conteúdo principal (alinha com a sidebar, ocupa o espaço restante). */
export const APP_PAGE_SHELL = "w-full min-w-0 max-w-7xl mx-auto space-y-6";

/** Igual ao shell, com mais espaço vertical entre blocos. */
export const APP_PAGE_SHELL_LOOSE = "w-full min-w-0 max-w-7xl mx-auto space-y-8";

/** Reporting / P&L — densidade enterprise. */
export const APP_PAGE_SHELL_PRO = "w-full min-w-0 max-w-[1600px] mx-auto space-y-5";
