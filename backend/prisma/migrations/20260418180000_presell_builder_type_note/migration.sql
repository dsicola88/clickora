-- PresellPage.type (TEXT) aceita o valor 'builder' para presells criadas no editor visual.
-- O documento do editor fica em content.pageDocument (JSON); não é necessária alteração de esquema.
SELECT 1;
